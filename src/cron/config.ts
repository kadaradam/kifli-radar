import type { AppCronConfig } from "../types";

export const config: AppCronConfig = {
  NEW_NOTIFICATION_THRESHOLD_IN_HOURS: 24,
  MIN_DISCOUNT_PERCENTAGE_FOR_ANALYTICS: 10,
  ANALYTICS_MAX_BATCH_SIZE: 25,
};
